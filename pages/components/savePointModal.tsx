import React, { useState } from "react";
import "antd/dist/antd.css";
import { Form, Modal, Radio, Switch } from "antd";

const SaveMatchInfoModal = () => {
  return <Modal title="Losing Point Information" destroyOnClose={true}></Modal>;
};

export default SaveMatchInfoModal;
